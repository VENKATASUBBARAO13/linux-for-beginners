# Navigation Commands

## Goal

Move confidently through the Linux filesystem.

## Commands and Concepts

| Command | Purpose |
| --- | --- |
| `pwd` | Print the current directory |
| `ls` | List directory contents |
| `ls -la` | Long listing including hidden files |
| `ls -lh` | Long listing with readable sizes |
| `cd path` | Change directory |
| `cd ~` | Go to your home directory |
| `cd ..` | Go to the parent directory |
| `cd -` | Return to the previous directory |

`/` is the filesystem root, `~` is your home, `.` is the current directory, and `..` is its parent.

## Practice Script

Read `practice.sh` first, then run:

```bash
bash practice.sh
```

## Exercise

Navigate from your home directory to `/tmp`, return with `cd -`, and list hidden files.

